import React from 'react';

const VolunteerDetail = (props) => (
    <div>[Volunteer Detail]: Show signed up request detail<br /><button className='btn btn-primary'>Sign up</button></div>
);

export default VolunteerDetail;